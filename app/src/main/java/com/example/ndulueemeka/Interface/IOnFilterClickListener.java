package com.example.ndulueemeka.Interface;

import android.view.View;

public interface IOnFilterClickListener {
    //on filter adapter filter click Listener
    void onClick(View view, int position);
}
